server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # Return 404 for API requests instead of index.html to avoid 405 errors
    location /api/ {
        return 404;
    }
}
